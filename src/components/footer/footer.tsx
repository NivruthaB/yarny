import { IFooterProps } from "./footer.interface"
import { FooterContainer } from "./footer.styled"

export const Footer = ({ scaleX }: IFooterProps) => {
    return (
        <FooterContainer>
            {/* <ProgressBar scaleX={scaleX} /> */}
        </FooterContainer>
    )
}